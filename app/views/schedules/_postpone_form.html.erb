<%= simple_form_for :schedule_postpone, url: schedule_postpone_path(schedule), method: :patch do |f| %>
  <% ot = schedule.next_occurring_time%>
  <%= f.datetime_field :time, value: (ot && ot.localtime.strftime("%Y-%m-%dT%H:%M:%S")) %>
  <%= f.button :submit %>
<% end %>