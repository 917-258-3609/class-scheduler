<p style="color: green"><%= notice %></p>

<h1>Schedules</h1>
<div id="calendar">
  <%= month_calendar(events: @calendar_occurrences) do |date, occurs| %>
    <h6><%= date.day %></h6>
    <% for o in occurs %>
      <%= o.occurable.subject_level.to_s %>
      <br/>
    <% end %>
  <% end %>
</div>
